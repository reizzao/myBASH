# TEST_INIT BASH

# DEFS
export EDITOR1="code"


# OPEN
alias dbash="cd $HOME/_myRZJ/myBASH"
alias dmusic="cd $HOME/Documentos/BC_Music"

# fmm() {
#   cd "~/Documentos/BC_Music"
# }


# FUNCTIONS
off() {
  sudo poweroff
  # desligar pc imediatamente
  # para desligar em 5 segundos: sudo shutdown -h +5
}

Ola() {
  echo "Olá Mundo 1000 !"
}

gpush() {
  git add . && git commit -m "$1" && git push
  # add + commitar e mandar pro remoto no mesmo comando git
}

v_git() {
  git --version
}

git_conf() {
  git config --global core.editor code
  # git config --global core.editor nano
}

gatt() {
  git pull && git push && git merge
}